---
- name: Sample usage docker_install_fedora Role
  hosts: fedoraserv
  become: true
  vars:
    ansible_user: tom
  roles:
    - role: docker_install_fedora
      vars:
        docker_users:
          - "{{ ansible_user }}"
